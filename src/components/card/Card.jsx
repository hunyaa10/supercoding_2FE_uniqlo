import React from "react";

const Card = (inpo) => {
  return (
    <div>
      <div>inpo.content.item_name</div>
      <div>inpo.content.price</div>
      <div>inpo.content.status</div>
    </div>
  );
};

export default Card;
